class Average:
    def __init__(self,numbers):
        numbers = []
    
    def total(self,numbers):
        for i in range(len(numbers)):
            total += numbers[i]

    def average(self,total,numbers):
        return total/len(numbers)
    
